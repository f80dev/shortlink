---
version: "2.0"
services:
  shortlinks:
    image: f80hub/shortlinks:latest
    expose:
      - port: 5000
        as: 5000
        to:
          - global: true
    env:
      - PASSWORD=oasis4271
profiles:
  compute:
    shortlinks:
      resources:
        cpu:
          units: 0.1
        memory:
          size: 250Mi
        storage:
          - size: 250Mi
  placement:
    dcloud:
      pricing:
        shortlinks:
          denom: uakt
          amount: 1000
deployment:
  shortlinks:
    dcloud:
      profile: shortlinks
      count: 1
