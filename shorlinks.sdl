---
version: "2.0"
services:
  shortlinks:
    image: f80hub/shortlinks:latest
    expose:
      - port: 80
        as: 80
        to:
          - global: true
    env:
      - PASSWORD=oasis4271
profiles:
  compute:
    shortlinks:
      resources:
        cpu:
          units: 0.1
        memory:
          size: 512Mi
        storage:
          - size: 512Mi
  placement:
    dcloud:
      pricing:
        shortlinks:
          denom: uakt
          amount: 1000
deployment:
  shortlinks:
    dcloud:
      profile: shortlinks
      count: 1
